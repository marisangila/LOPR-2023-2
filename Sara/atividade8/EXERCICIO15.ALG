Algoritmo "exercicio15"

Var
   numero, passe, i: inteiro


Inicio
      numero <- 1
      passe <- 1
      para i de 1 ate 11 passo 1 faca
           passe <- passe + 2
           numero <- numero + passe
           escreval(numero)
      fimpara
Fimalgoritmo