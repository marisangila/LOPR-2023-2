algoritmo "Nome do Programa"

var
    number: real
    med: real
    i: inteiro
inicio
    para i de 1 ate 8 faca
        escreval("Enter the",i,"° number:")
        leia(number)
        med <- med + number
    fimpara
    med <- med / 8

    escreval("The average these numbers are",med)

fimalgoritmo